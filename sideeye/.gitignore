# Run output (verdicts, reports) is regenerated — evidence, not source.
verdicts/
# Never commit anything resembling a key.
*.key
.env

# Codex isolated runtime home (session logs, blocked path)
live/codex-home/
